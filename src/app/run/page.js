import RunTrackerUI from "@/components/tracker/RunTrackerUI";

export const metadata = {
    title: "Run Tracker | ConsistencyGrid",
    description: "Track your running sessions natively.",
};

export default function RunTrackerPage() {
    return <RunTrackerUI />;
}
