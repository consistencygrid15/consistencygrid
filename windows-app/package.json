{
  "name": "consistency-grid-desktop",
  "version": "1.0.0",
  "description": "ConsistencyGrid Life Calendar Wallpaper Sync for Windows",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package": "electron-packager . ConsistencyGrid --platform=win32 --arch=x64 --out=dist --overwrite"
  },
  "author": "ConsistencyGrid",
  "license": "ISC",
  "devDependencies": {
    "electron": "^31.0.0",
    "electron-packager": "^17.1.2"
  },
  "dependencies": {
    "node-fetch": "^2.7.0"
  }
}
